{
  "$schema": "https://1sheng.io/schemas/license-entitlements/v1",
  "appId": "mission-control",
  "displayName": "Agent 指挥仓",
  "version": "1.0.0",
  "requiresSubscription": true,
  "description": "本文件声明 Agent 指挥仓（Mission Control）支持的授权维度。用户中心创建 SKU 时按 key 填写权益值；平台按 expiresAt 控制使用时长，按 entitlements 控制增值功能。",
  "entitlements": [
    {
      "key": "enableHumanWatch",
      "type": "boolean",
      "label": "人工值守",
      "description": "是否开通人工值守（专用值守 Agent、Worker 绑定、自动跟进与干预留痕）。",
      "default": false,
      "enforcement": "server",
      "enforcementPoint": "GET/POST /api/human-watch/* — requireHumanWatchEntitlement"
    },
    {
      "key": "enableLocalCliElevation",
      "type": "boolean",
      "label": "本地 CLI 提权",
      "description": "是否允许在聊天输入框使用「提权」按钮，临时以最高权限向边缘客户端下发 Codex/Claude 会话指令（绕过沙箱与审批）。",
      "default": false,
      "enforcement": "server",
      "enforcementPoint": "POST /api/chat/messages、/api/agents/message、/api/sessions/continue — local_cli_elevated"
    },
    {
      "key": "maxEdgeClients",
      "type": "number",
      "label": "最大边缘客户端连接数",
      "description": "允许同时连接到指挥仓 Bridge 服务器的边缘客户端（Tray 应用）数量上限。0 表示不限制（等同系统默认上限）。",
      "default": 0,
      "enforcement": "server",
      "enforcementPoint": "WebSocket bridge server — verifyClient handshake"
    }
  ]
}
