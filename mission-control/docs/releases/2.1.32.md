# 2.1.32

Date: 2026-07-07

## Changes

- Extends Bridge `steward_judge` wait time to 11 minutes so production human-watch steward replies are not failed while the Edge-side local CLI is still running.
- Keeps the 2.1.31 repeated short-reply fix and adds runtime protection for slow steward agents observed during production validation.
- Keeps server and Edge runtime manifests aligned for small-scope production verification.

## Verification

- Focused human-watch judge and memory sync tests must pass before publishing.
- Release surface verification must pass after syncing the Edge runtime bundle.
- Production validation should show `rule_evaluated=auto_send`, then `intervention_attempt`, then `intervention_completed=success` for binding 4.

## Notes

- Edge 2.1.32 is required for the matching in-flight steward Judge de-duplication and 10-minute local execution window.
