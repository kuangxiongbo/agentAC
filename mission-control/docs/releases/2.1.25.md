# E-AgentCenter 2.1.25

Date: 2026-07-06

## Changes

- Fixes human-watch event state convergence after automatic steward intervention.
- Auto-send success now marks the corresponding watch event as `resolved` with `resolved_by_type=steward_agent`.
- Grace-period, duplicate, and rate-limit skips no longer create new visible pending cards before the skip decision.
- Server image contents: embeds Edge runtime `2.1.25`; tray native version remains `3.0.7`.
- API/data/config changes: no schema migration.

## Verification

- `pnpm exec vitest run src/lib/__tests__/human-watch-orchestrator.test.ts src/lib/__tests__/human-watch-events.test.ts src/lib/__tests__/human-watch-events-route.test.ts` passed.
- `pnpm run typecheck` passed in `mission-control`.

## Rollback

- Roll back the 1Panel compose image tag to `agentcenter:2.1.24`, pull, and recreate `mission-control`.

## Known Risks

- Existing visible cards created by earlier versions may remain visible until ignored/resolved; new auto-send events should close automatically.
