# 2.1.48 - Managed Worker MCP Guard

Date: 2026-07-16

## Changes

- Supervision dispatch messages now carry the selected local Worker ID.
- Legacy scheduler paths re-check metadata and Goal relations immediately before mutating a task.
- Edge mailbox delivery is treated as platform-managed execution so Codex receives temporary supervision MCP configuration and bound Worker/session identity.

## Production Evidence

- Retained failed Goal `ac960dd7-84ed-4de5-9f1c-8ae68d9607d9` proved steward Agent 7 generated exactly three serial tasks and Worker 6 received the root prompt.
- Worker 6 correctly blocked because the mailbox resume path did not expose `mc_get_supervision_goal` or `mc_complete_supervision_task`.
- Tasks 19-20 recorded legacy auto-route and OpenClaw dispatch failures; the Goal was formally moved to failed and all evidence remains retained.

## Artifacts And Verification

- Server and Edge Runtime version: 2.1.48. Tray remains 3.0.8.
- Runtime ZIP SHA-256: `faa5ad767e62d28f899be42441889a5c2c1052ce84cc0aec146d53032c21b081`.
- Focused Server tests: 3 files / 8 tests. Focused Edge tests: 3 files / 22 tests.
- Server full suite passes: 126 files / 1094 tests. Edge full suite passes: 89 files / 953 tests. Both typechecks pass.
- Edge clean package build and Node 22.16.0 native ABI verification pass.
- Server clean build, image manifests, local installed Runtime update, and a new production Goal remain release gates.

## Upgrade And Risk

- Recreate only mission-control; no database migration is added.
- Tray automatic Runtime replacement remains a known risk. A successful manual restore does not validate automatic replacement.
- The 72-hour observation starts only after a new 2.1.48 Goal completes the full Worker MCP and steward verification chain.
