# 2.1.53 - Correction Message Worker Identity

Date: 2026-07-16

## Changes

- Goal correction and retry messages now include `worker_local_agent_id` in `session.continue.requested.payload`, matching initial Goal dispatch messages.
- Edge Runtime can therefore inject `MC_AGENT_ID` together with the existing Worker session and kind when a supervised task is retried or corrected.
- Docker release context excludes historical Runtime ZIPs and includes only the manifest-targeted 2.1.53 artifact, preventing duplicate multi-gigabyte public/standalone layers on resource-constrained production hosts.
- No database migration is added. Existing Goals, tasks, reliable messages, transcripts and audit events are retained.

## Production Evidence Before Fix

- Goal `6941af2d-3ae9-4350-a57c-2dc512227281` retry message `a9cb6d1e-0f5d-49db-bfac-a450f07c6dc2` was leased and acked by Runtime 2.1.52 in the original Worker session.
- Its `agent_ref` contained Worker 6, but its payload omitted `worker_local_agent_id`; Edge therefore could not build the complete managed Worker environment.
- Worker executed `pwd` but rejected completion because the required Goal-query ordering was not satisfied. No completion was fabricated and the 72-hour observation has not started.

## Verification

- Focused Server tests passed: 2 files / 6 tests. Focused Edge tests passed: 3 files / 6 tests.
- Full Server suite passed: 126 files / 1096 tests. Full Edge suite passed: 90 files / 955 tests.
- Server and Edge typechecks passed.
- Clean Edge Runtime build passed. Runtime ZIP SHA-256: `1609c1d5562b7e16ef82de2f62dddd86e8787c160462c1b4594ab01fc853702f`.
- Clean Server production build and `verify:release-surfaces` passed for Center/Runtime 2.1.53 and Tray 3.0.8.
- Resumed production Goal closure remains the final gate before the 72-hour observation starts.

## Upgrade And Risk

- Recreate only `mission-control`; no volume changes. Edge Runtime must update to 2.1.53; Tray remains 3.0.8.
- Roll back to 2.1.52 while retaining database volumes and all Goal evidence.
