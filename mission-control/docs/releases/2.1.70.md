# 2.1.70 - Runtime Artifact In Image

Date: 2026-07-23

## Changes

- Includes the current Edge Runtime ZIP in the Docker build context and production image.
- Adds a release gate that requires `.dockerignore` to allow the exact current Runtime filename.
- Retains the 2.1.69 Goal Worker 30-minute bounded runtime behavior.

## Artifacts And Upgrade

- Center image and Edge Web Runtime: `2.1.70`; native Tray remains `3.0.10`.
- Installed clients update through the Center manifest. No database migration is included.
- Roll back Center and Runtime to `2.1.68`; `2.1.69` must not be used because its image omitted the Runtime ZIP.

## Verification

- Release surfaces must verify the Docker allowlist, manifest, local artifact, SHA-256, and Runtime ZIP contents.
- Production acceptance must download the public URL as a ZIP, match its manifest SHA, install Runtime `2.1.70`, reconnect Bridge, and complete one real Goal Worker retry.

## Gray Release Risk

- Do not retry historical Goal tasks until both the local health API and Center report `2.1.70`.
