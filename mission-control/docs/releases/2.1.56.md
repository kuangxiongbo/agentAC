# 2.1.56 - Mailbox Codex Project Directory

Date: 2026-07-16

## Changes

- Reliable mailbox session continuation now refreshes the full local Agent record from `worker_local_agent_id` before resolving the execution directory.
- `enqueueLocalSessionPrompt` passes the refreshed Agent through cwd resolution, CLI execution options and prompt lifecycle events.
- Server and Edge implementations share the same behavior; no database migration is added.

## Production Evidence

- Goal `d8b117d1-1a20-4d61-8e6a-6f5bcede1c8c`, task 39 proved Agent 6 already stored `/Users/kuangxb/Desktop/agent指挥仓`, while the mailbox queue previously discarded the Agent and ran `pwd` from the Runtime directory.
- Task 39 submitted the mismatch as a real failed MCP completion. Tasks 40 and 41 remained dependency-held and were not activated.

## Verification

- Focused local-session executor suite passes, including the new mailbox regression test.
- Server and Edge typechecks, clean builds, release-surface verification, runtime artifact checksum and production Goal closure are release gates.
- A separate mailbox test may require rebuilding `better-sqlite3` when the developer shell Node ABI differs from the installed native module; release builds use the pinned supported Node version.
- Runtime ZIP SHA-256: `6264115144775aa2a78a5c4f640e81454abc5d1b9bd966b893c0eca682afb851`.
- Tray 3.0.8 DMG SHA-256: `32bf1379c108dbfdcbaeabab08fcddc83759a882f461239f1414eae4799b4817`.

## Upgrade And Risk

- Edge Runtime updates to 2.1.56. Tray remains 3.0.8; its default Center compatibility version advances to 2.1.56 without a native tray release.
- Roll back to 2.1.55 while retaining database and audit volumes. The failed 2.1.55 Goal remains an audit record.
