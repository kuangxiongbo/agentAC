# 2.1.74 - Work Detail Identity Consistency

Date: 2026-07-24

## Changes

- Makes the Agent main-detail endpoint use the same unified Work identity resolver as tasks, activity, files, SOUL and memory.
- Returns live Bridge detail for historical cloud mirror IDs after reconnect, including `source=bridge_index`, `bridge_online=true` and `detail_live=true`.
- Preserves Workspace isolation and bounded offline index fallback.

## Artifacts And Upgrade

- Center image and Edge Web Runtime: `2.1.74`; native Tray remains `3.0.10`.
- No database migration is required. Roll back Center and Runtime together to `2.1.73` if required.

## Verification

- Focused route tests cover a historical ID returning live Bridge main detail and local task/activity/resource data.
- Production verification must compare the historical ID and current sync-index ID against the same local Work.

## Gray Release Risk

- Global projections remain follow-up work; public certificate-chain validation remains an infrastructure risk.
