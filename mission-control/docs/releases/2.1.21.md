# E-AgentCenter 2.1.21

Date: 2026-07-05

## Changes

- Fixed Edge local bootstrap client identity drift: when an installed client already has a `mc-edge-*` client id, local bootstrap now preserves that id instead of hashing it again.
- Keeps human-watch bindings stable across tray/runtime restarts, so queued `human_watch.assist.requested` messages continue to target the same Edge client.
- Carries forward 2.1.20's mailbox judge-session fix for historical steward agents.

## Production Verification

- Published after real production validation of message `96678b01-0911-49dd-b80c-6a62cbccb7f1`.
- Result: `completed`, `delivered=true`, steward session `f15d9598-e0a0-4b13-b05e-bf23f8b46076`, steward reply returned successfully.
- Resource posture: production build remains off-host; the small production server only pulls the prebuilt image and runs with container memory limits plus swap.

## Upgrade Notes

- Deploy `agentcenter:2.1.21` and keep `latest` aligned.
- Installed Edge clients should fetch runtime `2.1.21`; this is required to prevent client-id drift after restart.
