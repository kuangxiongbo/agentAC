# E-AgentCenter 2.1.27

Date: 2026-07-06

## Changes

- Changes the human-watch auto-send safety throttle from a 1-hour window to a 24-hour window.
- Keeps the default maximum at `60` successful steward interventions per binding per 24 hours.
- Adds `max_interventions_window_seconds=86400` to tenant human-watch rules while preserving the existing `max_interventions_per_hour` field as the configured maximum count for backward compatibility.
- Updates the human-watch rules UI label from hourly limit to 24-hour limit.
- Server image contents: embeds Edge runtime `2.1.27`; tray native version remains `3.0.7`.
- API/data/config changes: no schema migration.

## Verification

- `pnpm exec vitest run src/lib/__tests__/human-watch-defaults.test.ts src/lib/__tests__/human-watch-orchestrator.test.ts` passed.
- `pnpm run typecheck` passed in `mission-control`.
- `pnpm run verify:release-surfaces` passed.

## Rollback

- Roll back the 1Panel compose image tag to `agentcenter:2.1.26`, pull, and recreate `mission-control`.

## Known Risks

- The maximum count field name remains `max_interventions_per_hour` for compatibility, but the effective default window is now 24 hours.
