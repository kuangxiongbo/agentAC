# 2.1.61 - Workspace Agent Registration Hotfix

Date: 2026-07-22

## Changes

- Bridge Agent registration now resolves the owning Workspace from `sync_clients` and upserts on `(name, workspace_id)`.
- Central inventory reconciliation and duplicate cleanup use the same resolved Workspace instead of Workspace 1.
- Gateway POST Agent registration uses the composite Agent identity introduced by migration `071`.
- Docker artifact gates require the package-local sanitizer and current Edge Runtime ZIP inside the build context.

## Artifacts And Upgrade

- Center image and Edge Web Runtime: `2.1.61`; native Tray remains `3.0.8`.
- Upgrade immediately from `2.1.60` when Bridge logs contain `ON CONFLICT clause does not match any PRIMARY KEY or UNIQUE constraint`.
- Schema is unchanged from `2.1.60`; rollback code/image is `2.1.60`, while `2.1.59` plus its pre-migration database backup remains the schema rollback path.

## Verification

- Migration-backed cross-Workspace Agent upsert passed; Server 1138 tests, Edge 994 tests, both typechecks/builds, release surfaces and artifact boundaries passed. Image digest and production Bridge evidence remain release gates.
