# 2.1.67 - Reliable Supervision Execution Progress

Date: 2026-07-23

## Changes

- Acknowledges reliable session continuation and Human Watch assist messages only after the Edge CLI turn completes; execution failures now produce a real fail result instead of a false completed message.
- Limits Goal supervision to one active task per Edge client/session, including shared-session Agent index entries.
- Adds task last-activity and stale-state display plus a remote transcript process viewer that refreshes every five seconds while a task is active.

## Artifacts And Upgrade

- Center image and Edge Web Runtime: `2.1.67`; native Tray remains `3.0.9`.
- No database migration or native Tray reinstall is required. Tray `3.0.9` installs the checksum-verified Runtime from the existing manifest flow.
- Roll back Center and Edge Runtime to `2.1.66`. Back up SQLite before resetting any false-running gray Goal tasks.

## Verification

- Server full suite: 1143/1143 passed; focused supervision suite: 35/35 passed.
- Edge full suite: 999/999 passed; focused mailbox/session suite: 25/25 passed; both TypeScript checks and production builds passed.
- Production acceptance requires one reset gray task to produce a new Worker transcript turn before its reliable message reaches completed.

## Gray Release Risk

- Existing tasks falsely marked in progress by older Runtime versions require an explicit one-time reset; the release does not mutate historical task state automatically.
