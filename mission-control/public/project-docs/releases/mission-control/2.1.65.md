# 2.1.65 - Human Watch Entitlement Runtime Repair

Date: 2026-07-23

## Changes

- Includes `license-schema.json` in both production image paths so Human Watch entitlement checks can run at runtime.
- Adds a release boundary gate that fails when the required entitlement schema is omitted.
- Uses a single build worker and a 3 GB Node heap for resource-limited cross-architecture image builds; runtime behavior is unchanged.
- Preserves Human Watch decision, goal supervision, task, session synchronization and local-mode business behavior.

## Artifacts And Upgrade

- Center image and Edge Web Runtime: `2.1.65`; native Tray remains `3.0.9`.
- No database migration or native Tray reinstall is required.
- Roll back to `2.1.64` using the pre-deployment Compose and SQLite backups if needed.

## Verification

- License config and Human Watch event creation must not raise `/app/license-schema.json` ENOENT.
- Production acceptance requires a real Claude MCP watch event, reliable assist delivery, completed intervention and a semantically relevant Steward reply in the same Worker transcript.
- Server/Edge tests, typechecks, builds, release surfaces and image boundary checks must pass.

## Gray Release Risk

- Human Watch binding `4` is enabled only for bounded acceptance and must be disabled after evidence collection.
