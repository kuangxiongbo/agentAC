# 2.1.92 - Scoped User-Center Self-Signed TLS

Date: 2026-07-30

## Changes

- Adds `MC_USERCENTER_TLS_INSECURE=1` for self-signed user-center HTTPS.
- Applies the opt-in to license verification, tenant context/onboarding, and license schema push.
- Keeps Edge/Tray TLS and OIDC TLS settings independent and preserves strict validation by default.

## Validation

- Center typecheck and focused user-center tests pass.
- Production acceptance requires public user-center authorization to succeed with the scoped flag enabled.

## Upgrade And Rollback

- Upgrade Center and Edge Runtime to 2.1.92. Tray remains 3.0.10.
- Rollback to 2.1.91 is data-compatible.
