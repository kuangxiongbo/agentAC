# 2.1.86 - Human Watch Loop Protection

Date: 2026-07-28

## Changes

- Stops repeated judge calls and mailbox attempts while the same Worker fingerprint already has an active watch event.
- Fixes Edge automatic dedicated-session provisioning deadlock caused by nested acquisition of the same Agent serialization key.
- Extends auto-stop regression coverage for successful count, runtime duration, rate-limit skips and re-enable windows.

## Validation

- Focused Human Watch loop suite: 25 tests passed.
- Center and Edge typecheck plus focused lint passed. Full tests, builds, Runtime startup and production checks remain release gates.

## Upgrade And Rollback

- Upgrade Center and Edge Runtime to 2.1.86. No database migration; Tray remains 3.0.10.
- Rollback to 2.1.85 is data-compatible.

## Gray Risk

- Active escalations intentionally suppress repeated judge attempts until the event is resolved, dismissed or expired.
