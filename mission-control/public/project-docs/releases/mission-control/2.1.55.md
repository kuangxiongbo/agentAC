# 2.1.55 - Persisted Codex Project Directory

Date: 2026-07-16

## Changes

- Bound Codex session resumes now prefer the persisted Agent `mc_session_project_path` over the Edge Runtime process directory.
- Server and Edge executor implementations use the same cwd precedence contract.
- A regression test covers a bound Codex session whose caller cwd differs from its persisted project directory.

## Production Evidence

- Goal `fb757be7-05b6-44a7-833f-28472e69916b` proved three tasks were created once, strictly serialized, automatically activated by the supervision monitor, and bound to Worker 6 and session `019ed56d-1f38-7983-b79c-87543322e549`.
- Tasks 36 and 37 completed through `mc_complete_supervision_task` with `ok=true`; schema 3 Goal reads were complete and remained below transport limits.
- Task 38 honestly failed because `git rev-parse HEAD` ran from the non-Git Edge Runtime directory. The failed Goal is retained as the root-cause audit record.

## Verification

- Edge local-session executor tests pass: 19 tests. Full Server suite passes: 126 files / 1097 tests. Full Edge suite passes: 90 files / 956 tests.
- Server and Edge typechecks pass. Clean Edge Runtime build passes with Node 22 native ABI validation.
- Runtime ZIP SHA-256: `032e1beb6a2797870e9301d73ef21031b54e8e288ceb73453f094d75393cc35c`.
- Tray 3.0.8 DMG SHA-256: `7cbc5f3b4b8442e146481f0a1df2303c4d76d1b39c05e94d5464d312746ccc32`.
- Release-surface verification, clean Server build, image manifest checks and post-deployment Goal closure remain release gates.

## Upgrade And Risk

- Edge Runtime updates to 2.1.55. Tray remains 3.0.8.
- Repository-based supervised goals must bind the Worker Agent to an existing repository path before dispatch.
- No database migration is added. Roll back to 2.1.54 while retaining database and audit volumes.
