# 2.1.51 - Managed Steward Session Continuation

Date: 2026-07-16

## Changes

- `mc_continue_session` now declares write, non-destructive, idempotent and closed-world MCP annotations.
- Non-interactive steward supervision can continue an existing managed Worker session without an unavailable approval prompt.
- The existing center-side prompt fingerprint remains the idempotency boundary, so retrying the same session and prompt does not enqueue duplicate Worker input.
- Session termination, arbitrary command execution and high-risk permission decisions keep their existing approval boundaries.

## Production Evidence Before Fix

- Goal `93620542-0a56-49e6-8626-034c53330b0e` returned the complete schema 2 snapshot for tasks 27-29 and showed dependency holds without legacy scheduler pollution.
- Its root reliable mailbox message was delivered, but the steward's follow-up `mc_continue_session` call returned `user cancelled MCP tool call`, including after explicit user approval.
- `pwd` was not executed and no completion evidence was submitted, so the Goal is retained as blocked evidence and does not start the 72-hour observation.

## Verification

- Focused Server MCP tests passed: 1 file / 3 tests.
- Focused Edge MCP tests passed: 1 file / 3 tests.
- Full Server suite passed: 126 files / 1096 tests. Full Edge suite passed: 89 files / 953 tests.
- Server and Edge typechecks passed. Edge typecheck used one Node process with a 10 GiB old-space limit.
- Edge Runtime and Server clean production builds passed.
- `client-runtime-2.1.51-darwin-aarch64.zip` has SHA-256 `5c7f9477c5d67971f53b6823bea8ca3bb856d3beb8032c0ece794d36cc023d52`; its packaged MCP script contains all four continuation annotations.
- `verify:release-surfaces` passed for Server 2.1.51, Edge Runtime 2.1.51 and Tray 3.0.8.
- Production Goal closure remains the post-deployment gate.

## Upgrade And Risk

- Recreate only `mission-control`; no database migration is added.
- Installed clients must update Web Runtime to 2.1.51 because the MCP tool declaration is packaged in Runtime. Tray remains 3.0.8.
- This remains a small production gray release. Start the 72-hour observation only after all three Worker tasks submit real MCP evidence and the steward independently verifies the Goal.
- Roll back to image 2.1.50 and Runtime 2.1.50 while retaining database volumes, Goals and audit records.
