# 2.1.51 - Managed Session Continuation Approval

Date: 2026-07-16

## Edge Changes

- `mc_continue_session` now exposes non-destructive, idempotent and closed-world annotations to managed Codex sessions.
- A steward running non-interactively can send a bounded follow-up to an existing Worker session; identical retries remain deduplicated by the center queue.

## Verification

- Focused Edge MCP tests passed: 1 file / 3 tests.
- Full Edge suite passed: 89 files / 953 tests. Typecheck and clean Runtime production build passed.
- `client-runtime-2.1.51-darwin-aarch64.zip` has SHA-256 `5c7f9477c5d67971f53b6823bea8ca3bb856d3beb8032c0ece794d36cc023d52`; its packaged MCP script contains all four continuation annotations.
- `verify:release-surfaces` passed for Server 2.1.51, Edge Runtime 2.1.51 and Tray 3.0.8.
- Production continuation remains the post-deployment gate.

## Upgrade

- Installed clients must update Web Runtime to 2.1.51 and verify local health reports `version=2.1.51`.
- Tray remains 3.0.8; no native application update is required.

## Known Risk

- MCP approval success alone is insufficient. Production must show real Worker command output, successful completion submission, automatic successor activation and independent Goal verification before observation starts.
