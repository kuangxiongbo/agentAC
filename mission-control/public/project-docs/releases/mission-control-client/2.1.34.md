# 2.1.34 - Human Watch MCP Trigger UI Alignment

Date: 2026-07-07

## User / Operator Changes

- Aligns the Edge Web runtime version with the center release.
- No local runtime behavior change is required for this patch; managed Worker sessions already include the instruction to call `mc_create_watch_event` when follow-up confirmation or user-style response is needed.

## Runtime / Tray Notes

- Edge Web runtime package version: `2.1.34`.
- Native tray version is unchanged.
- Existing installations can keep running; runtime update is only needed for version-surface consistency.

## Verification

- Center release surface verification checks the embedded Edge runtime manifest and artifact hash.
- Installed Edge health should report `2.1.34` after runtime update.

## Gray Release Risks

- This patch does not change local MCP injection code; any Worker that does not receive the managed-session preamble may still rely on the server fallback scan path.
