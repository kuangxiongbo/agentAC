# 2.1.33

Date: 2026-07-07

## Changes

- Fixes Codex steward Judge reply recovery for long sessions where the fixed transcript page has the same assistant count after a new reply.
- Treats a newer assistant timestamp as a valid new Judge reply even when the text repeats, for example `继续`.
- Preserves the 10-minute steward Judge local CLI timeout, duplicate in-flight guard, and 5-minute Bridge heartbeat tolerance from 2.1.32.

## Verification

- `pnpm exec vitest run src/lib/__tests__/human-watch-judge.test.ts`
- `pnpm run typecheck`
- Installed/standalone Edge runtime should report `2.1.33` and connect to the production Bridge before the production human-watch validation.
