# 2.1.46 - Edge Supervised Worker Center Bridge

Date: 2026-07-15

## Edge Changes

- Adds same-origin proxy routes for authoritative center Goal reads and supervised Worker completion submission.
- Adds MCP tools `mc_get_supervision_goal` and `mc_complete_supervision_task` without exposing the center API key in Worker CLI arguments; completion identity is bound to managed process environment values and cannot be overridden through tool arguments.
- Excludes `metadata.goal_id` from all task queue polling paths in addition to the existing legacy scheduler isolation.

## Production Evidence

- Worker 6 received the 2.1.45 root task and produced valid version evidence, but queried an empty Edge-local task table because Goal tasks are center-owned.
- Center task queue polling claimed dependency tasks 13 and 14 outside Goal Dispatcher and failed them through the legacy execution path.
- Failed Goal `925630ce-802e-4217-a7f0-9101cf33c8f0` remains retained.

## Artifacts

- Edge runtime version: 2.1.46.
- Tray remains 3.0.8 and reuses the existing verified DMG.
- Runtime ZIP SHA-256: `d39e1c4b5a8e7cde8e0a0f5fe5e1f5ebaa9937e741ae22f296455d0f8e5467e6`.

## Verification

- Edge queue and scheduler isolation suite passes: 2 files and 4 tests.
- Full Edge suite passes: 89 files and 953 tests.
- Edge TypeScript check and production runtime build pass.
- MCP `tools/list` exposes both new supervision tools.
- Runtime ABI/package and release-surface checks pass. Installed update, CSS check, Bridge reconnect, and production Goal evidence remain release gates.

## Upgrade

- Keep tray 3.0.8 installed. It fetches the 2.1.46 center manifest and replaces the Edge runtime.

## Gray Release Risks

- The Edge-local API key must remain valid for proxying Worker evidence to the center.
- Validate against a new Goal ID; do not reuse either retained failed Goal.
