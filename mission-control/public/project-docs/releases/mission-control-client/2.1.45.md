# 2.1.45 - Edge Supervision Scheduler Isolation

Date: 2026-07-15

## Edge Changes

- Edge legacy auto-route, assigned dispatch, stale requeue, and Aegis review exclude every task carrying `metadata.goal_id`.
- Auto-route uses a metadata-aware compare-and-set before assignment.
- Supervised tasks synchronized from the center remain controlled exclusively by Goal Dispatcher.

## Production Evidence

- In 2.1.44, the center bundle correctly excluded supervised tasks, but the Edge runtime copy of `task-dispatch.ts` did not.
- Edge auto-routed dependency-blocked tasks 10 and 11 and synchronized repeated `spawn openclaw ENOENT` failures back to the center.
- Goal `f4ea59ee-2e40-48af-8cd5-4bc0f67d4943` is retained as failed audit evidence.

## Artifacts

- Edge runtime version: 2.1.45.
- Tray version remains 3.0.8 and reuses the existing verified DMG.
- Runtime ZIP SHA-256: `31d3c2b549d9be3f81b1dafe2c7e49b1562b244293cf1e6655700c3b4aaa8fdc`.

## Verification

- Edge supervised scheduler isolation tests pass: 3 tests.
- Reliable mailbox session continuation test passes.
- Full Edge suite passes: 88 test files and 951 tests.
- Edge TypeScript check passes.
- Packaged standalone bundle contains all `metadata.goal_id` guards.
- Runtime ABI, package manifest, CSS asset, installed update, Bridge reconnect, and production Goal evidence remain release gates.

## Upgrade

- Keep tray 3.0.8 installed. It fetches the 2.1.45 center manifest, replaces the Edge runtime, and restarts the local service.

## Gray Release Risks

- Existing failed Goal task records must not be rewritten or deleted.
- Validate against a new Goal ID; do not reuse the 2.1.44 failed Goal.
