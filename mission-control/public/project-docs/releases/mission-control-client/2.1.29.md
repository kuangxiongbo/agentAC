# Edge Runtime 2.1.29 Release Notes

Date: 2026-07-06

## Changes

- Sends `framework`, `parent_id`, and `session_key` in manual Bridge `status_push` payloads.
- Keeps the Edge runtime package version aligned with the 2.1.29 center release.

## Verification

- `pnpm run typecheck` passed in `mission-control-client`.

## Rollback

- Repoint the server Edge runtime manifest to `2.1.28`, or roll the server image back to `agentcenter:2.1.28`.
