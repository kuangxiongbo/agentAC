# 2.1.78 - Edge Metrics Authority

Date: 2026-07-24

## Changes

- Adds the read-only `agent_metrics` capability for local diagnostics, attribution, evaluation, and token/cost facts.
- Reuses the same local computation functions for Edge HTTP routes and Bridge responses.
- Adds aggregate Agent evaluation output and the missing `session-costs` response.

## Upgrade

- Native Tray remains `3.0.10` and installs Runtime `2.1.78` from the Center manifest.
- No local database migration or user configuration change is required.
- Roll back the Runtime and Center together to `2.1.77`.

## Verification

- Edge full suite `1006/1006`, focused Bridge tests `9/9`, and typecheck passed.
- Packaged Runtime clean startup and release-surface validation remain release gates.

## Gray Release Risk

- Token snapshots preserve existing source limits and are sent only to the authenticated Center Bridge.
