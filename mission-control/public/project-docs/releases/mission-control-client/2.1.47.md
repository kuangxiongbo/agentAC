# 2.1.47 - Clean Edge Runtime Build

Date: 2026-07-16

## Edge Changes

- Runtime packaging removes `.next` before the production build to prevent stale scheduler and route chunks from entering the ZIP.
- Edge continues to use `metadata.goal_id` isolation because center Goal relation tables are intentionally not stored in the local Edge database.
- MCP supervision tools and managed Worker identity binding from 2.1.46 remain unchanged.

## Production Evidence

- Runtime 2.1.46 exposed both supervision MCP tools and reconnected to the center with zero sync backlog after a checksum-verified manual restore.
- Tray 3.0.8 automatic replacement downloaded the 2.1.46 artifact but removed the installed runtime before completing replacement; the old orphan process continued serving 5101 from a deleted cwd.

## Artifacts

- Edge runtime version: 2.1.47.
- Tray remains 3.0.8 and reuses the existing verified DMG.
- Runtime ZIP SHA-256: `e6f2341766682d409230d12d8b6cb825819501bfa0ddef4aa3a606243346605e`.

## Verification

- Edge metadata scheduler and queue isolation focused tests pass locally.
- Full suite passes: 89 files / 953 tests. Typecheck passes with an 8 GiB Node heap.
- Clean runtime packaging passes after deleting `.next`; staged `better-sqlite3` loads on Node 22.16.0 and the ZIP SHA matches the center manifest.
- CSS, installed update recovery, Bridge reconnect, and production Goal evidence remain release gates.

## Upgrade

- Keep tray 3.0.8 installed. It fetches the 2.1.47 center manifest.
- If automatic replacement leaves the runtime directory empty, restore only from the published ZIP whose SHA matches the center manifest; retain the Edge data directory and configuration.

## Gray Release Risks

- Automatic Tray runtime replacement remains under investigation and must not be treated as verified merely because a manual restore succeeds.
