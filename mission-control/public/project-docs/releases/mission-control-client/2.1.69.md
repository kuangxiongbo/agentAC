# 2.1.69 - Goal Worker Runtime Budget

Date: 2026-07-23

## Changes

- Goal Worker messages can carry a 30-minute execution timeout for long document, code, and verification work.
- Edge clamps server-provided timeouts to a safe 10-second through 2-hour range.
- Reliable mailbox status remains `processing` until the local CLI exits; errors and timeouts are reported as failed.

## Upgrade

- Update Edge Web Runtime to `2.1.69`. Native Tray remains `3.0.10` and does not require reinstalling.

## Verification

- Reliable session continuation tests pass, including successful execution, failure reporting, duplicate suppression, and timeout clamping.
- TypeScript validation passes under Node 22.

## Gray Release Risk

- Retry old failed Goal tasks only after the Runtime health endpoint and Center manifest both report `2.1.69`.
