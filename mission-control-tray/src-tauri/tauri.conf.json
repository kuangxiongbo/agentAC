{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "E-Agent Edge",
  "version": "2.0.3",
  "identifier": "work.1sheng.e-agent-edge",
  "build": {
    "beforeDevCommand": "mkdir -p dist && cp tray-panel.html setup.html dist/ 2>/dev/null || (mkdir -p dist && cp tray-panel.html dist/)",
    "beforeBuildCommand": "mkdir -p dist && cp tray-panel.html setup.html dist/",
    "frontendDist": "../dist"
  },
  "app": {
    "withGlobalTauri": true,
    "macOSPrivateApi": true,
    "windows": [],
    "security": {
      "csp": null,
      "capabilities": ["default", "setup", "tray-panel", "edge-shell"]
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "macOS": {
      "infoPlist": "Info.plist"
    },
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "shortDescription": "E-Agent Edge client tray",
    "longDescription": "Downloads edge runtime on first launch and keeps mission-control-client running."
  }
}
