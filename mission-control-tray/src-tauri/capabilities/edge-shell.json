{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "edge-shell",
  "description": "Hidden macOS shell window converted to NSPanel",
  "windows": ["edge-shell"],
  "permissions": ["core:default"]
}
