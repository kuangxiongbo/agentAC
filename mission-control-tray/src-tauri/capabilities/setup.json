{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "setup",
  "description": "Edge connection setup window",
  "windows": ["setup"],
  "permissions": ["core:default", "opener:default"]
}
