# 任务拆解-人工值守 V2：结构化事件与决策闭环

## 1. 执行原则

- 先文档和模型评审，再实现。
- 第一阶段优先复用 `permission_requests`，不要一次性大迁移。
- 所有恢复动作必须有审计。
- 不允许普通聊天回复改变审批状态。
- 人在 Worker 会话内回复时，必须由 Worker/本地托管端回写平台统一决策 API，平台完成状态流转和审计。
- 默认由值守 Agent 判断并提交结构化决策/指令；只有命中高危动作清单时升级到人工通知地址。
- 规则引擎默认只发现事件，不直接代发。

## 2. 任务列表

| 编号 | 任务 | 阶段 | 状态 | 说明 |
|------|------|------|------|------|
| HW2-001 | PRD V2 文档 | P0 | 已完成 | `PRD-人工值守-结构化事件与决策闭环.md` |
| HW2-002 | 架构设计 V2 文档 | P0 | 已完成 | `架构设计-人工值守-结构化事件与决策闭环.md` |
| HW2-003 | 测试用例 V2 文档 | P0 | 已完成 | 本文档配套测试用例 |
| HW2-004 | 评审现有 `permission_requests` 字段是否足够承载 watch event | P0 | 已完成 | 当前采用独立 `human_watch_events` 表，并保持与 `permission_requests` 关联 |
| HW2-005 | 定义 WatchEvent TypeScript 类型和转换器 | P0 | 已完成 | 已新增统一事件类型、视图与 store 模型 |
| HW2-006 | API 返回 watch event 视图 | P0 | 已完成 | 已提供 `/api/human-watch/events` 与动作 API |
| HW2-007 | 审计事件枚举和写入函数 | P0 | 已完成 | 已落 `watch_event_created/visible/claimed/closed` |
| HW2-008 | MCP 工具增加 watch event 别名 | P1 | 待开始 | 保留 permission request 工具 |
| HW2-009 | 托管 Worker prompt 更新为 watch event 语言 | P1 | 待开始 | 更明确触发条件 |
| HW2-010 | 值守 Agent prompt 更新为只能工具决策 | P1 | 待开始 | 禁止只回复同意 |
| HW2-011 | UI 待处理队列改名/聚合为值守事件 | P1 | 已完成 | 已增加值守事件面板，支持实时查看与操作 |
| HW2-012 | 规则引擎 event-driven 模式 | P1 | 已完成 | 规则命中时先创建结构化事件，不直接代发 |
| HW2-013 | legacy auto-send 灰度开关 | P1 | 待开始 | 老租户兼容，默认关闭 |
| HW2-014 | 决策后 bridge_continue 恢复器 | P1 | 待开始 | 仅 suspected_stuck 使用 |
| HW2-015 | Gateway approval 审计补齐 | P1 | 待开始 | 记录 resume_attempted/succeeded/failed |
| HW2-016 | 高危动作策略与通知地址配置 | P1 | 待开始 | 删除/卸载/覆盖/生产部署/付款/外部发送/密钥访问/提权等动作清单 |
| HW2-016a | WatchNotificationService | P1 | 待开始 | platform inbox / webhook 先行 |
| HW2-016b | 高危通知审计 | P1 | 待开始 | sent / failed / acknowledged |
| HW2-016c | Worker 会话内人工回复同步 | P1 | 待开始 | `worker_human_reply_sync` / API 回写平台统一决策 |
| HW2-016d | 决策幂等与并发冲突控制 | P1 | 待开始 | idempotency key / event_version / 409 |
| HW2-016e | 通知失败重试和告警 | P1 | 待开始 | retry / failed audit / 不自动通过 |
| HW2-016f | 通知地址脱敏与安全存储 | P1 | 待开始 | token/address 不进明文日志 |
| HW2-017 | 新增 `watch_events` / `watch_event_audit` migration | P2 | 已完成 | 已落地 `human_watch_events` 表与事件审计字段 |
| HW2-018 | CSV/审计导出 | P2 | 待开始 | 统一事件导出 |
| HW2-019 | E2E：Worker MCP create/wait/decide | P1 | 已完成 | 已完成人工值守脚本与真实本地会话回写验证 |
| HW2-020 | E2E：规则命中不直接代发 | P1 | 已完成 | 已验证规则命中先生成事件，再由平台决策 |
| HW2-021 | 可观测性指标 | P2 | 待开始 | pending/latency/failed/conflict 指标 |
| HW2-022 | 边缘离线重连一致性 | P2 | 待开始 | 旧回复不能覆盖新决策 |
| HW2-023 | 服务状态离线客户端删除 API | P1 | 进行中 | 删除 `sync_clients + sync_agent_index + source=client agents + sync_sessions`，有绑定时阻断 |
| HW2-024 | 服务状态离线客户端删除 UI | P1 | 进行中 | 仅离线客户端显示删除，要求二次确认串 |
| HW2-025 | 非客户端镜像主列表自动收敛 | P1 | 进行中 | 无 `client_id` 的 `client/bridge/bridge_index` 不长期占位 |
| HW2-026 | 原生弹窗替换为统一模拟弹窗 | P1 | 待开始 | 服务端与客户端所有确认/输入危险操作统一替换 |
| HW2-027 | 智能体删除来源边界梳理与实现 | P1 | 待开始 | 区分客户端同步、实时索引、值守、本地主数据删除路径 |
| HW2-028 | 客户端删除链路自测与回归 | P1 | 待开始 | 覆盖成功删除、绑定阻断、错误提示展示 |

## 3. 推荐实施顺序

```text
HW2-004 → HW2-005 → HW2-006 → HW2-007
HW2-008 → HW2-009 → HW2-010
HW2-011
HW2-012 → HW2-013 → HW2-014
HW2-015 → HW2-016 → HW2-016a → HW2-016b → HW2-016c
HW2-016d → HW2-016e → HW2-016f
HW2-019 → HW2-020
HW2-023 → HW2-024 → HW2-028
HW2-025 → HW2-027
HW2-026
```

## 4. 最小可发布版本定义

最小可发布版本不要求新增 `watch_events` 表，但必须满足：

1. `permission_requests.context_json.watch_event` 能表达来源、类型、恢复方式。
2. UI 用“值守事件”统一展示 pending 请求。
3. Worker MCP 主动创建和等待决策路径可用。
4. 值守 Agent MCP 决策路径可用。
5. 规则模式下不直接代发，先创建事件。
6. 决策和恢复有审计。
7. 命中高危动作清单时触发人工通知地址；通知缺失时阻断恢复并进入平台待审批。
8. 人在 Worker 会话回复后，平台能收到同步并关闭审批；未同步时不得误判完成。
9. 并发审批、通知失败、过期回复都有明确错误、审计和不恢复保障。
10. 离线客户端可在服务状态中删除，且删除后不再有镜像智能体和会话快照占位。
11. 所有危险确认和输入确认串交互必须使用统一模拟弹窗，不允许浏览器原生弹窗。

## 5. 自测命令建议

```bash
cd mission-control
pnpm exec vitest run \
  src/lib/__tests__/permission-requests.test.ts \
  src/lib/__tests__/permission-request-exec-bridge.test.ts \
  src/lib/__tests__/human-watch-*.test.ts

cd ../mission-control-client
pnpm exec vitest run \
  src/lib/__tests__/codex-mcp-injection.test.ts \
  src/lib/__tests__/local-cli-permission.test.ts
```

## 6. 未决问题

1. 是否在 V2.0 立即新增 `watch_events` 表，还是先用 `permission_requests.context_json.watch_event`。
2. 高危动作清单第一版是否还需要补充更多动作类型。
3. 高危动作审批是否需要双人审批。
4. 旧租户的 `auto_send` 是否保留默认开启，还是迁移时统一关闭。
5. 删除离线客户端时，是否需要同时导出审计快照再删除会话快照。
