# 测试报告-2.1.68-Edge 运行时恢复与监督链路

## 1. 范围

- 版本：AgentCenter / Edge Runtime `2.1.68`，Tray `3.0.10`。
- 目标：修复不完整 standalone fallback 覆盖有效 Runtime、旧 LaunchAgent 重复争抢 `5101`，恢复 Goal Worker 监督执行基础链路。
- 日期：2026-07-23。

## 2. 自动化结果

- Server `verify:release-surfaces`：通过。
- Server / Edge TypeScript：通过。
- Server supervision：10 files，35/35 通过。
- Edge Mailbox / local executor：3 files，25/25 通过。
- Tray Rust：8/8 通过，包括无效 fallback 不覆盖当前目录。
- Runtime 打包：Node 22 ABI 127、干净 ZIP 解压、58 个 Next compiled server 文件、真实健康启动全部通过。

首次 Edge 测试命令误用了系统 Node ABI 141，而原生模块已按 Runtime Node 22 ABI 127 重建，导致 4 个加载失败；切换到发布规定的 Node 22 后同组 25/25 通过。该次失败不计为产品行为通过证据。

## 3. 真实安装证据

- DMG 安装版本：Tray `3.0.10`。
- 安装后 Runtime：`2.1.68`，`GET /api/status?action=health` 返回 `healthy`。
- 必需文件：`app-route-turbo.runtime.prod.js` 存在，Next compiled server 文件 58 个。
- 静态资源：聊天页 CSS 返回 `HTTP 200` 与 `Content-Type: text/css`。
- 进程：3 分钟观察期间 Tray 1 个、`5101` listener 1 个、`EADDRINUSE` 0 次。
- 旧服务：`work.1sheng.e-agent-edge.runtime` launchd job、plist 和 `runtime-node.pid` 均已清理。
- Bridge：`connected=true`，目标 `wss://agent.1sheng.work/bridge-ws`，`reconnectAttempts=0`，`lastError=null`。
- 浏览器：本地 `/chat` 正常加载，显示 `v2.1.68`、中心服已连接和真实会话列表。

## 4. 生产待验收

1. 发布并部署 `2.1.68` 镜像后，将本地 Runtime manifest 从临时验证地址恢复为中心默认地址。
2. 确认中心 Runtime/Tray manifest 的版本和哈希与本报告一致。
3. 备份中心和 Edge SQLite，重置 Goal `c33fbd18-24de-4932-825d-a20703b2aece` 的假进行中任务 46/48。
4. 确认只激活一个任务、Worker transcript 出现新轮次、CLI 返回后可靠消息才 completed。
5. 在中心监督页打开“查看过程”，核对 transcript、最后活动时间和状态，不以普通聊天是否复制系统任务日志作为判断依据。
