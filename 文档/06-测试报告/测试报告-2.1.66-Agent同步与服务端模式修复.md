# 测试报告-2.1.66-Agent 同步与服务端模式修复

## 范围

- 生产中心服务端模式恢复。
- 模式切换遗留 Bridge Agent 重复记录清理与 API 去重。
- Edge Agent 创建、更新、删除、同步和状态变化实时上报。
- HTTP heartbeat inventory 在 clients-only 模式下更新中心索引。

## 自动验证

- Server typecheck、生产构建：通过。
- Edge typecheck、生产构建与 Runtime 打包：通过。
- Server 完整测试：1142/1142 通过；heartbeat 合同与 Agent 同步聚焦测试：13/13 通过。
- Edge 完整测试：997/997 通过；Bridge/Gateway 聚焦测试：4/4 通过。
- Release surface 与 artifact boundary 门禁：通过。
- Edge Runtime ZIP SHA-256：`0991a5e49dbd6d4815d2eec13e172f9154daa5851c297051064da2dff4f152bd`。

## 生产预验证

- AgentCenter `centralMode=true`，服务状态 `1/1`。
- 精确删除 9 条可由实时索引替代的遗留 `source=bridge` 行，保留正式 Edge Agent、会话和值守绑定。
- 本地 Agent “宣传视频制作”通过真实 Bridge `status_push` 后进入中心 `sync_agent_index`，生产页面只显示一次。

## 最终验收

- 发布 2.1.66 后由 Tray 更新 Edge Runtime。
- 新建临时 Agent，不重连 Bridge，验证中心快速出现且只出现一次；随后删除并验证中心收敛。
