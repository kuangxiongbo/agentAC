# 2.1.60 上游安全回迁测试报告

日期：2026-07-22

状态：预发布门禁通过，生产真实值守与 Goal Supervisor 证据待灰度部署后补录。

## 1. 验证范围

- 上游 Mission Control v2.2.0 行为级安全回迁批次 A-E。
- Server/Edge 执行沙箱、Workspace 所有权、审计隔离、迁移、供应链与 standalone 产物边界。
- Center/Edge Runtime 2.1.60 发布面；原生 Tray 保持 3.0.8。

## 2. 全量自动化结果

| 门禁 | 结果 |
|---|---|
| Server `pnpm test` | 134 files，1137 tests 全部通过 |
| Edge `pnpm test` | 98 files，994 tests 全部通过 |
| Server/Edge `pnpm run typecheck` | 通过 |
| Server/Edge `pnpm run build` | 通过，Node 22.16.0 |
| `verify:release-surfaces` | 通过，Center/Runtime 2.1.60、Tray 3.0.8 |
| `check-artifact-boundaries.mjs` | 通过，双端 standalone `server.js` 存在 |

Edge Runtime ZIP SHA-256：`d22094e537853e6889b43647c023643b25a0c8c122f4c0e987bc823f4b07172b`。

首次真实 Docker build 在 Next 编译后发现 sanitizer 使用仓库外相对路径，容器内报 `MODULE_NOT_FOUND /scripts/sanitize-standalone-artifact.mjs`，镜像未推送。修复后 Server/Edge 各自从 Docker build context 内调用 sanitizer；边界检查新增包内脚本与根脚本一致性及 build 命令路径约束，Edge 重新生产构建和打包通过。

## 3. 生产数据库迁移演练

演练数据来自生产 2.1.59 SQLite 在线只读备份，未停止或修改生产实例。远端和本地原始数据库 SHA-256 均为 `f4b3c4a8ac485ec6f5dce6cd624bb52279d23b20302cdaf8710c605b305e5be3`。

- 迁移 `071_workspace_audit_and_agent_identity`、`072_workspace_isolation_policy` 连续执行两次通过。
- `integrity_check=ok`；迁移前后外键违规均为 0。
- Agent 迁移前后均为 20 个字段，`node_id/framework/parent_id/working_memory` 等定制字段无丢失。
- 同名 Agent 可存在于不同 Workspace；同一 Workspace 重名被唯一约束拒绝。
- `audit_log.workspace_id`、Workspace `brand/isolation` 均存在。
- 从备份恢复的独立副本 `integrity_check=ok`、外键违规 0；远端/本地压缩包哈希一致。

## 4. 生产待验证项

- 镜像 `2.1.60` 与 `latest` 摘要、linux/amd64 平台一致。
- 灰度实例健康、版本、迁移、磁盘/内存和 Bridge 连接稳定。
- Human Watch 在真实 Worker 问询后 5 秒内产生完整介入事件、可靠消息回执和同会话语义回复。
- Goal Supervisor 生成至少两个有依赖任务，完成派发、受管 MCP 完成、纠偏/独立验收并到达 completed。
- 已安装 Edge Runtime 更新到 2.1.60，Tray 保持 3.0.8，聊天 CSS 资源类型正确。
