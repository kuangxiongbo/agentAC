# 2.1.65 值守授权产物修复测试报告

## 1. 根因

`2.1.64` 已恢复 Runtime ZIP 交付，Claude 也真实调用了 `mcp__mission_control__mc_create_watch_event`，但中心返回 HTTP 500。生产日志为 `ENOENT: /app/license-schema.json`：安全回迁后的 entitlement 代码需要该文件，Docker Runtime 阶段却没有复制它。

## 2. 修复

- 标准镜像和 runtime-only 镜像都显式复制 `license-schema.json`。
- 产物边界脚本增加强制检查。
- 生产跨架构构建限制为 1 worker / 3 GB Node heap，避免 8 GB Docker VM 出现 OOM；仅影响构建阶段。
- 不修改人工值守判断、回复、目标监督、任务或会话业务代码。

## 3. 验收

- 自动化：Server/Edge 单测、typecheck、build、release surfaces、artifact boundary。
- 镜像：容器内 schema 文件存在，license config 正常。
- 真实链路：Claude MCP 调用、watch event、assist lease/ack/completed、intervention success、Steward 同会话语义回复。

## 4. 自动化结果

- Server 值守/授权聚焦测试：6 files / 31 tests passed。
- Edge MCP/值守聚焦测试：4 files / 11 tests passed。
- Server/Edge typecheck passed；Server/Edge production build passed。
- Release surfaces 与标准/runtime-only artifact boundary passed。

## 5. 生产结果

- Git：`390eb0e`；镜像 `2.1.65` 与 `latest` 摘要 `sha256:8a479e336b635b0cfb3eb8e6e3ba777086b220b23f9a486e162e2f2c861209b0`。
- 生产容器健康版本 `2.1.65`，`/app/license-schema.json` 可读；本地 Runtime `2.1.65`、Bridge connected。
- Worker 8 / Claude session `d501e3e5-07f7-44d3-8235-311a57ccf967` 调用 `mcp__mission_control__mc_create_watch_event` 成功。
- 事件 `6fb6f19c-93d0-4507-aa05-025a2326ffb5` resolved；干预 `34621` 为 `intervention_completed / success`。
- 值守回复：`A（只读通过）。本次验收全程未执行文件修改或生产变更，值守通信链路验证已通过。`
- 回复通过 MCP tool result 和后续 user message 写回同一 Worker transcript，Worker 明确复述选择及理由。
- 测试 binding `4` 已关闭；生产内存约 1.2/1.8 GiB、根盘约 61%。

结论：人工值守 Agent 已能在真实生产环境代替人对 Worker 等待问题作出基于上下文的语义回复。
