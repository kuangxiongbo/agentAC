# 测试报告-人工值守

## 1. 状态

- 实现状态：**2.1.51 发布前自动化通过，生产闭环待验证**
- 报告更新时间：2026-07-16

## 2. 执行记录

### 2.1 自动化单测（mission-control）

```bash
cd mission-control && pnpm exec vitest run src/lib/__tests__/human-watch-*.test.ts
```

| 套件 | 条数 | 结果 |
|------|------|------|
| human-watch-audit | 6 | 通过 |
| human-watch-policy | 3 | 通过 |
| human-watch-bindings | 2 | 通过 |
| human-watch-rules | 2 | 通过 |
| human-watch-orchestrator | 5 | 通过 |
| **合计** | **18** | **通过** |

### 2.2 用例映射（HW-TC-A01～A06）

| 用例 ID | 覆盖方式 | 结果 |
|---------|----------|------|
| HW-TC-A01 代发成功留痕 | orchestrator 单测 `auto-sends...` | 通过 |
| HW-TC-A02 代发失败留痕 | orchestrator 单测 `failed continue` | 通过 |
| HW-TC-A03 指纹去重 | orchestrator 单测 + audit 幂等 | 通过 |
| HW-TC-A04 Bridge 离线 | orchestrator 单测 `bridge_offline` | 通过 |
| HW-TC-A05 租户隔离 | audit 单测 `isolates tenants` + API `tenant_id` 过滤 | 通过 |
| HW-TC-A06 Worker 详情 UI | 干预记录 Tab（手工验收待 E2E） | 待 E2E |

### 2.3 手工/E2E（待跑）

- 中心服 `centralMode` + Bridge 在线 + 租户开通 → 创建值守 → 绑定 → Worker 会话卡住 → 查 `human_watch_interventions` 与 UI Tab
- `POST /api/human-watch/evaluate` 手动触发单条 binding
- `GET /api/export?type=human_watch_interventions&format=csv`（admin）

### 2.4 2.1.51 值守目标监督回归

| 验证项 | 结果 |
|--------|------|
| Server 聚焦 MCP/权限测试 | 3 files / 19 tests，通过 |
| Edge 聚焦 MCP/权限测试 | 2 files / 6 tests，通过 |
| Server 全量测试 | 126 files / 1096 tests，通过 |
| Edge 全量测试 | 89 files / 953 tests，通过 |
| Server/Edge typecheck | 通过 |
| Server/Edge Runtime 干净生产构建 | 通过 |
| 发布表面校验 | 2.1.51 / Runtime 2.1.51 / Tray 3.0.8，通过 |
| Runtime ZIP | SHA-256 `5c7f9477c5d67971f53b6823bea8ca3bb856d3beb8032c0ece794d36cc023d52`；已核对内置 `mc_continue_session` annotations |
| 生产 Goal | `93620542-0a56-49e6-8626-034c53330b0e` 待 2.1.51 部署后继续；当前不得计为通过 |

## 3. 残余风险

| 风险 | 说明 |
|------|------|
| E2E 未跑 | 真实 Bridge + 边缘 CLI 全链路需联调环境 |
| `rule_evaluated` 量 | 高频 transcript 可能产生较多 noop 行；后续可加「仅 matched 写入」开关 |
| L4 判官 | HW-018：`steward_judge_request`；`llm_enabled` 时摘要→判官→代发 |
| LLM 扫漏 | HW-019：`llm_sweep_enabled` + `llm_sweep` 留痕 |

## 4. 修订

| 版本 | 日期 | 说明 |
|------|------|------|
| V1.0 | 2026-05-19 | 18 条单测通过；HW-TC-A01～A05 有自动化覆盖 |
| V2.0 | 2026-07-16 | 记录 2.1.51 双端全量测试、构建、制品校验；生产三任务闭环仍待部署验证 |
