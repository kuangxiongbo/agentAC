# 需求状态-客户端主智能体识别与聊天聚类

## 1. 需求状态

- 当前状态：进行中
- 当前阶段：功能已实现，待补全全仓验证
- 更新时间：2026-04-17

## 2. 已完成

1. 主题 PRD 已创建。
2. 测试用例文档已创建。
3. 架构设计文档已创建。
4. 程序设计文档已创建。
5. 任务拆解文档已创建。
6. 测试报告文档已初始化。
7. 主智能体运行时检测已扩展到 `claude/codex/openclaw/cursor/opencode/hermes`。
8. 主智能体安装、检测、同步链路已打通。
9. 创建向导已限制为仅显示已识别主智能体类型。
10. 子智能体默认挂载到主智能体，服务端创建接口带自动兜底。
11. 聊天会话列表已按主智能体类型分组。
12. 创建向导弹层布局已修复，底部下一步按钮恢复可见。
13. 无 `openclaw.json` 时，同步配置已改为安全跳过。
14. 删除 `agent + workspace` 已按 framework 做保护，非 OpenClaw 类型不再误走 OpenClaw 删除链路。
15. `source=local` 的磁盘导入智能体已从主智能体层中分离，改为独立分组显示。
16. `.agents` 类型的磁盘导入智能体现在会优先推断具体 framework，无法推断时明确标记为 `unknown`。
17. 本地样例 `local-expert` 已补齐为 `claude` 类型元数据。
18. `Claude / Codex` 已具备通过本地 `session_key` 接任务与收消息的执行闭环。
19. `Hermes / Cursor / OpenCode` 已接入统一 executor adapter。
20. `wake`、任务广播、通知派送已接入本地 runtime 分流，不再默认误走 OpenClaw gateway。
21. `Cursor` 已具备通过本地 session 续跑执行能力。
22. `OpenCode` 已具备可执行闭环；当前安装版本若缺少 session run 能力，会自动回退到单次 prompt 执行。
23. `Hermes` 已接入统一本地执行入口，待本机安装 Hermes CLI 后即可验证真实执行闭环。
24. 本地子智能体现在默认采用专属主会话绑定策略，首次消息或任务可自动懒创建 dedicated session。
25. `Claude / Cursor / Codex` 子智能体已具备“直接发消息/发任务即进入各自独立上下文”的执行闭环。
26. 命令面板对 `/api/agents/message` 的请求字段已修复，不再误报 `Validation failed`。
27. dedicated 本地子智能体的主会话现在会在首次建立时自动注入角色定义 bootstrap。
28. dedicated 本地子智能体在角色定义变更后会自动切换到新的专属会话，避免旧会话残留旧角色。
29. 对无效旧 `session_key` 绑定已增加自动自愈，下一次发送时会自动重建 dedicated 主会话。

## 3. 进行中

1. 清理仓库存量 typecheck 错误后补完整验证。

## 4. 下一步

1. 如需全仓 `typecheck` 通过，先处理 `login/page.tsx` 和 `gateway-sync.ts` 的存量类型错误。
2. 如需把 `OpenCode / Hermes` 也推进到 dedicated 主会话自动 provision，继续补 runtime 原生建会话桥接层。
3. 如需把 `Cursor / OpenCode` 的“可执行”进一步推进到“可会话管理”，继续补 session 扫描、转录读取和会话面板接入。
4. 如需把角色治理做完整，可继续补 `role_hash / session_bootstrap_state / last_session_error` 的可视化展示。
5. 如需增强控制面，继续完善主智能体安装器与任务派发体验。
