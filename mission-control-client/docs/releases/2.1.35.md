# 2.1.35 - Human Watch Binding Session Kind Persistence

Date: 2026-07-07

## User / Operator Changes

- Aligns the Edge Web runtime version with the center release.
- Adds the local migration surface for `human_watch_bindings.worker_session_kind` so local tables stay compatible with the center schema.

## Runtime / Tray Notes

- Edge Web runtime package version: `2.1.35`.
- Native tray version is unchanged.
- Installed Edge runtimes should update to `2.1.35` to keep local schema/version surfaces aligned.

## Verification

- `pnpm run typecheck` in `mission-control-client`.
- Center release surface verification checks the embedded Edge runtime manifest and artifact hash.

## Gray Release Risks

- This patch does not change local steward judge behavior. It supports the center-side binding/session-kind persistence fix and keeps schema compatibility for installed Edge runtime updates.
