# 2.1.71 - Goal Retry State Alignment

Date: 2026-07-23

## Changes

- Aligns the Edge Web Runtime version with the Center patch that resumes blocked Goals during Worker retry or reassignment.
- Keeps the 30-minute bounded Goal Worker execution and completion-aware ACK behavior introduced in prior releases.

## Upgrade

- Keep native Tray `3.0.10`; allow it to install Edge Web Runtime `2.1.71` from the Center manifest.

## Verification

- Confirm local health reports `2.1.71`, Bridge reconnects, and a supervised Worker completion is visible in the task transcript before the center message completes.

## Gray Release Risk

- This patch changes Center correction state handling only; no native Tray behavior changes.
