# 2.1.60 - Bounded Edge Execution And Workspace Ownership

Date: 2026-07-22

## Changes

- Edge executes supervised and reliable mailbox prompts with effective Agent/task tool, budget and cwd limits.
- Existing Claude/Codex sessions, new dedicated sessions and role bootstrap all use the same dispatch sandbox.
- Local and remote session paths honor shared/strict Workspace policy without blocking an owned managed Worker.
- Remote Skill downloads are size bounded and atomically installed; Agent workspace writes are atomic and symlink-safe.
- Standalone packaging removes environment files, development sources and duplicated public assets.

## Upgrade And Verification

- Installed Web Runtime updates to `2.1.60`; native Tray remains `3.0.8`.
- Runtime ZIP must contain `server.js`, `.next/static/chunks`, MCP scripts and `src/lib/schema.sql`, with no `.env*` or `.npmrc`.
- Verify Bridge reconnect, session transcript/continue, Human Watch response and Goal Supervisor dispatch after update.
- Pre-release verification passed 994 Edge tests, typecheck, production build and runtime artifact boundary checks.

## Rollback

- Re-select Center/Runtime `2.1.59`; retain local Agent/session data. Restore the local SQLite backup only when reverting schema behavior is required.
