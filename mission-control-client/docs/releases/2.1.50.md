# 2.1.50 - Compact Supervision Reads

Date: 2026-07-16

## Edge Changes

- `mc_get_task` is declared read-only, non-destructive, idempotent and closed-world, allowing a non-interactive Worker to query one task when it needs a fallback check.
- `mc_get_supervision_goal` continues to use process-bound Worker/session identity and now receives the center's bounded schema 2 Goal snapshot.

## Verification

- Focused Edge MCP tests passed: 1 file / 3 tests.
- Server contract tests prove the bounded response retains all task states and dependencies while excluding oversized metadata/evidence/action fields.
- The full Edge suite passed: 89 files / 953 tests. Edge typecheck and the clean Runtime production build passed.
- `client-runtime-2.1.50-darwin-aarch64.zip` has SHA-256 `f8c8f575f0ed5fb418226ed8560f50970829049d9d028230b57e95069ca75c2d`; release-surface verification confirmed static chunks and the manifest contract.
- Installed-user update and a new production Goal remain rollout gates.

## Upgrade

- Installed clients must update the Web Runtime to 2.1.50 and verify the local health endpoint reports `version=2.1.50`.
- Tray remains 3.0.8; no native application update is required.

## Known Risk

- A new production Goal must prove all serial tasks can query, execute and submit evidence before the 72-hour observation starts.
- Failed Goal `d4d17ffb-c19f-465c-852f-44979c34aff2` remains audit evidence and must not count as closure success.
