# 2.1.101

- Adds Agent-level `codex_model_provider` routing for managed Codex sessions.
- Applies the provider override on both new dedicated sessions and resumed sessions.
- Allows the default steward to use `OpenAI3` independently of the global Codex relay.
- Rejects malformed provider identifiers before CLI invocation.
- Tray remains 3.0.10; only the embedded Edge Web Runtime is updated.
- Focused tests passed 28/28 and Edge TypeScript validation passed.
