# 2.1.63 - Managed Claude Human Watch Tools

Date: 2026-07-22

## Changes

- Managed Claude Worker sessions now discover Human Watch and Goal Supervisor MCP tools without changing the user's global Claude configuration.
- Both new and resumed Claude sessions receive Worker/session identity and local Runtime authentication.
- This fixes Claude asking a confirmation question but treating `mc_create_watch_event` as a missing shell command.

## Upgrade

- Edge Web Runtime updates to `2.1.63`; native Tray remains `3.0.9`.
- Tray `3.0.9` will download and atomically activate the new Runtime. No native app reinstall is required.

## Verification

- Unit tests cover managed and unmanaged argument construction plus Runtime API-key inheritance.
- Runtime ZIP SHA-256: `c0751addb5612eb7023c66a89ce80e493ebc57c08c6e100a879276c37e8417d2`.
- Real CLI validation must return `MCP_AUTH_OK` after calling `mc_session_transcript` with no global MCP profile.
- Final production validation must prove the Steward reply is written into the same Claude Worker session.
