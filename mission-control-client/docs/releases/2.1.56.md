# 2.1.56 - Mailbox Codex Project Directory

Date: 2026-07-16

## Changes

- Mailbox-delivered managed Worker continuations now reload the full Agent configuration before resuming Codex.
- The configured `mc_session_project_path` is preserved through the queue and supplied directly as the resume process cwd.
- Worker identity and managed MCP injection remain unchanged.

## Upgrade And Verification

- Installed clients update Web Runtime to 2.1.56. Tray remains 3.0.8.
- Verify the runtime package, MCP server, manifest and Center compatibility version all report 2.1.56.
- Run a repository-bound supervised Goal and verify `pwd`, `node --version` and `git rev-parse HEAD` complete serially through MCP without `cd` or `git -C`.
- Runtime ZIP SHA-256: `6264115144775aa2a78a5c4f640e81454abc5d1b9bd966b893c0eca682afb851`; Tray 3.0.8 DMG SHA-256: `32bf1379c108dbfdcbaeabab08fcddc83759a882f461239f1414eae4799b4817`.

## Known Risk

- A missing or deleted configured directory still follows the existing safe fallback and must be treated as a failed repository validation rather than silently corrected by the Worker.
